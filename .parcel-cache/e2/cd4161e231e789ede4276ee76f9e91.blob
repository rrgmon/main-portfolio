'use strict';
const cardEl = document.getElementsByClassName('card');
const cardPopEl = document.getElementsByClassName('card-pop');
function cardPopDisplayNone() {
}
let mediaQueryEl = window.matchMedia('(max-width: 56em)');
function widthDetect(mediaQueryEl1) {
    if (mediaQueryEl1.matches) for(let i = 0; i < cardEl.length; i++){
        cardEl[i].addEventListener('mouseover', ()=>{
            for(let i1 = 0; i1 < cardPopEl.length; i1++)cardPopEl[i1].classList.add('card-pop-display');
        });
        cardEl[i].addEventListener('mouseout', ()=>{
            for(let i1 = 0; i1 < cardPopEl.length; i1++)cardPopEl[i1].classList.remove('card-pop-display');
        });
    }
    else for(let i1 = 0; i1 < cardEl.length; i1++){
        cardEl[i1].removeEventListener('mouseover', cardPopDisplayFlex);
        cardEl[i1].removeEventListener('mouseout', cardPopDisplayNone);
    }
}
mediaQueryEl.addEventListener('change', ()=>{
    widthDetect(mediaQueryEl);
});
mediaQueryEl.addEventListener('onload', ()=>{
    widthDetect(mediaQueryEl);
});
widthDetect(mediaQueryEl);
