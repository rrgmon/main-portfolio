// 'use strict';
// const cardEl = document.getElementsByClassName('card');
// const cardPopEl = document.getElementsByClassName('card-pop');
// let i = 0;
// function cardPopDisplayFlex() {
//   while (i < cardPopEl.length) {
//     cardPopEl[i].classList.toggle('card-pop-display');
//     i++;
//   }
// }
// // function cardPopDisplayNone() {
// //   cardPopEl.classList.remove('card-pop-display');
// // }
// let mediaQueryEl = window.matchMedia('(max-width: 56em)');
// function widthDetect(mediaQueryEl) {
//   if (mediaQueryEl.matches) {
//     while (i < cardEl.length) {
//       cardEl[i].addEventListener('mouseover', cardPopDisplayFlex);
//       cardEl[i].addEventListener('mouseout', cardPopDisplayFlex);
//       i++;
//     }
//   } else {
//     while (i < cardEl.length) {
//       cardEl[i].removeEventListener('mouseover', cardPopDisplayFlex);
//       cardEl[i].removeEventListener('mouseout', cardPopDisplayFlex);
//       i++;
//     }
//   }
// }
// mediaQueryEl.addEventListener('change', () => {
//   widthDetect(mediaQueryEl);
// });
// mediaQueryEl.addEventListener('onload', () => {
//   widthDetect(mediaQueryEl);
// });
// widthDetect(mediaQueryEl);
'use strict';
const cardEl = document.getElementsByClassName('card');
const cardPopEl = document.getElementsByClassName('card-pop');
function cardPopAdd() {
    for(let i = 0; i < cardEl.length; i++){
        cardEl[i].addEventListener('mouseover', ()=>{
            cardPopEl[i].classList.add('card-pop-display');
        });
        cardEl[i].addEventListener('mouseout', ()=>{
            cardPopEl[i].classList.remove('card-pop-display');
        });
    }
}
function cardPopRemove() {
    for(let i = 0; i < cardEl.length; i++){
        cardEl[i].removeEventListener('mouseover', ()=>{
            cardPopEl[i].classList.add('card-pop-display');
        });
        cardEl[i].removeEventListener('mouseout', ()=>{
            cardPopEl[i].classList.remove('card-pop-display');
        });
    }
}
const mediaQueryEl = window.matchMedia('(min-width: 56em)');
function widthDetect(mediaQueryEl1) {
    if (mediaQueryEl1.matches) cardPopAdd();
    else cardPopRemove();
}
mediaQueryEl.addEventListener('change', ()=>{
    widthDetect(mediaQueryEl);
});
mediaQueryEl.addEventListener('onload', ()=>{
    widthDetect(mediaQueryEl);
});
widthDetect(mediaQueryEl);
